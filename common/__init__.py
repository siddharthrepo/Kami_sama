"""Shared utilities for the Hindi and Nepali corpus pipelines.

Only *code* is shared between the two languages. Data, tokenizers, vocabularies and
model weights are kept strictly separate per language, as the assignment requires.
"""
